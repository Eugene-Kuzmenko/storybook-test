import React from 'react';
import Button from './Button';

export default {
  title: 'Button',
}

const createSizeStory = size => () => (

)

export const small = () => (
  <Button size="tiny">
    Tiny
  </Button>
);

